// => Snapshot d'un composant React
// Prenez un snapshot du rendu d'un composant Button et vérifiez qu'il correspond au snapshot précédemment enregistré.

const Button = (label: string) => `<button>${label}</button>;`;
export default Button;
