// => Vérification de l'associativité de la multiplication
// Objectif : Tester que la multiplication est associative, c'est-à-dire que (a * b) * c est égal à a * (b * c).
const multiply = (a: number, b: number): number => a * b;
// Indication : Utilisez trois arguments a, b, et c.
