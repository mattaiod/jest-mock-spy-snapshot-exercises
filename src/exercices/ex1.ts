// => Mock d'une fonction fetch
// Mockez la fonction fetch pour qu'elle retourne une promesse résolue avec { data: "valeur" }.
// // Exemple de requête à mocker
// fetch('https://api.example.com/data')
//   .then(response => response.json())
//   .then(data => console.log(data));

//   Indice : fetch est une fonction globale dans les environnements de navigateur ou Node.js, elle se trouve donc dans global.fetch.
