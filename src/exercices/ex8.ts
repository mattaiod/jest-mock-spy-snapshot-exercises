// => Mock d'une fonction avec des paramètres variés
// Mockez une fonction subtract(a, b) qui retourne la soustraction de deux nombres et vérifiez qu'elle est appelée avec les bons arguments.
// // Fonction à mocker
// const subtract = (a, b) => a - b;
