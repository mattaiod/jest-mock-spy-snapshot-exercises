// // => Mock d'une API de création
// Mockez une API de création createUser qui retourne un utilisateur créé avec id, name, et email.
// // Fonction à mocker
// const createUser = (name, email) => {
//   return { id: Math.random(), name, email };
// };
